# Copy this file to .env and fill in your key:
#   cp .env.example .env
#
# .env is gitignored, so your key never gets committed. The script reads this
# automatically on startup (a real exported env var still takes precedence).
GOOGLE_PLACES_API_KEY=your_key_here
